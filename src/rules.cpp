#include <iostream>
#include <string>

#include "rules.h"
// #include "game.h"

bool Rules::isValid(const Game&) {

    return true;
}

bool Rules::gameOver(const Game&) {

    return true;
}

bool Rules::roundOver(const Game&) {

    return true;
}
