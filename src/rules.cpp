#include <iostream>
#include <string>

#include "rules.h"
#include "game.h"

bool isValid(const Game&) {

    return true;
}

bool gameOver(const Game&) {

    return true;
}

bool roundOver(const Game&) {

    return true;
}
